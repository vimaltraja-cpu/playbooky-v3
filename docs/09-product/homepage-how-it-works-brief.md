# How it works — visual brief

Status: Current
Scope: Homepage `#how-it-works`

## Reference

WorkshopPilot process section: left process rail + right circular stage, scroll-driven active step, stacked on mobile.

## Desktop (≥960px)

- Eyebrow `PROCESS` + Newsreader headline
- Tall scroll track (`steps × 90vh`)
- Sticky split: left numbered steps with vertical spine; right orbit stage
- Scroll progress selects active step (muted → full)
- Orbit: double ring + pill labels + real product beat inside

## Beats (real components)

1. Describe — `AIComposer`
2. AI works through it — recommendation-loading illustrations
3. Get your playbook — `RecommendationCardReveal`
4. Build and run — builder `ActivityCard`s + guide list

## Mobile

Stacked chapters, no sticky walkthrough / no orbit labels.
