# Components

This document will track the approved component library for PlayBooky V3.

It will list component categories, component status, usage rules, dependencies, accessibility requirements, and the process for moving a component from draft to current and approved.
